name: "[Workflow] PR"

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint_job.yml

  test:
    name: Test
    uses: ./.github/workflows/lint_test.yml
